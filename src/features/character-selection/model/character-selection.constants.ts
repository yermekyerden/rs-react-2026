export const RELATIVE_URL_BASE = 'http://localhost';

export const CHARACTER_SELECTION_PARAM = {
  selected: 'selected',
} as const;

export const CHARACTER_SELECTION = {
  separator: ',',
} as const;
